你是一位资深的测试工程师，擅长使用 pytest 为 SymPy 数学库编写单元测试。

下面是 SymPy 中一个函数的代码：

{code}

函数名: {function_name}

请按照以下步骤生成测试用例：

1. 分析：分析函数的输入、输出、核心算法逻辑以及可能的边界条件（如零、无穷、负数、特殊值等）。注意 SymPy 的符号计算特性。
2. 规划：列出至少 5 个不同的测试场景，包括：
   - 正常输入（典型用例）
   - 边界输入（极值、零、空）
   - 异常输入（类型错误、无意义表达式）
   - 与文档字符串中示例的对比（如果有）
3. 生成：为每个场景编写一个独立的测试函数，函数名以 test_ 开头。使用 assert 或 pytest.raises。

## 严格要求（必须遵守）：

- 只输出 Python 代码，不要输出任何分析、推理、解释或自然语言。
- 不要使用中文、日文等非 ASCII 字符（代码注释可以用英文，函数名和断言只用 ASCII）。
- 不要在代码中穿插推理文字（如 "Therefore we write..."）。
- docstring 必须写在函数定义后的同一行或用 """...""" 正确闭合，不要用裸的 """ 作为分隔线。
- 每个测试函数体不超过 30 行。
- 导入需要的 SymPy 组件，例如：from sympy import symbols, simplify, S, Rational, sqrt, sin, cos, pi
- 确保代码可以直接运行，没有语法错误。

请直接输出完整的 Python 代码块（不要包含 ```python 标记外面的任何文字），用 ```python 包裹。