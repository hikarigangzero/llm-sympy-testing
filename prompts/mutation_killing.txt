你是一位资深的软件测试工程师，擅长变异测试（Mutation Testing）。

下面是函数 {function_name} 的原始代码：

```python
{original_code}
```

现在，代码中引入了以下变异：

{mutant_description}

你的任务是：编写一个或多个测试用例，能够 **检测（杀死）** 这个变异。也就是说，你写的测试必须在原始代码上通过，在变异代码上失败。

已有测试（可能无法杀死此变异）：

```python
{existing_tests}
```

请按照以下步骤思考：

1. 分析：这个变异改变了什么行为？什么输入会让原始代码和变异代码产生不同的输出？
2. 设计：构造能暴露差异的具体输入。
3. 实现：编写 pytest 测试函数。

要求：
- 测试函数名以 test_ 开头，用清晰的注释说明它针对哪个变异。
- 使用 assert 断言精确的期望输出。
- 仅输出新增的测试函数，不要重复已有测试。
- 确保代码可以直接运行。

请输出完整的 Python 代码块，用 ```python 包裹。
